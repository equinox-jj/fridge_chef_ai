/// Layout constants for the mobile-first product UI.
abstract final class AppLayout {
  /// Mobile design width — cap content on larger screens.
  static const double screenMax = 430;

  /// Minimum touch target size.
  static const double tapTarget = 44;

  /// Bottom tab bar height.
  static const double navHeight = 64;

  /// Top header / app bar height.
  static const double headerHeight = 56;

  /// Screen horizontal padding.
  static const double gutter = 20;

  /// Width of the focus ring stroke.
  static const double focusRingWidth = 3;
}
